#include "type_item.h"

TypeItem::TypeItem(const QIcon& icon, const QString& text, QWidget *parent)
    : QWidget(parent)
{
	QHBoxLayout* layout = new QHBoxLayout(this);
	layout->addItem()
}

TypeItem::~TypeItem()
{

}
